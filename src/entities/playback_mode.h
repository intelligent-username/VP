/*
    Playback Mode Entity
    Represents the different playback viewing modes
 */

#ifndef PLAYBACK_MODE_H
#define PLAYBACK_MODE_H

typedef enum {
    PLAYBACK_MODE_FULLSCREEN,   /* Full-screen playback */
    PLAYBACK_MODE_MINIMIZED     /* Minimized (quarter-size) playback */
} PlaybackMode;

#endif
